<?php
$sql = "SELECT * FROM /*Usertabelle*/;";
$stmt = mysqli_stmt_init($conn);
if (!mysqli_stmt_prepare($stmt, $sql)) {
    /* Fehlermeldung */
}
mysqli_stmt_execute($stmt);

/* Überprüfung ob zumindest ein User in der Datenbank ist */

/* alle User ausgeben mithilfe einer while Schleife ausgeben */

//TODO: User updaten

$sql = "UPDATE /* Usertabelle */ SET  = /* gender = ?, firstname = ?, ... */ WHERE userId = ?";
$stmt = mysqli_stmt_init($conn);
if (!mysqli_stmt_prepare($stmt, $sql)) {
    /* Fehlermeldung */
}

/* Parameter an das Statement binden */
